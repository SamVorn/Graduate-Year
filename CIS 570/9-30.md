## Can we survive with R format only?

## Decoding Machine Language
- how do we convert 1's and 0s to assembly language and to C code
> Machine language => assembly => C

- For each 32 bits:
1. look at the opcode to distinguish between r format, j format, and i format
2. use instruction set format to decode

